#include<istream>
using namespace std;

class Door{
private:
	bool isOpen;
	bool isClose;
public:
 	Door()
	{
	   constructor funtion
	}

	void openDoor()
	{
	   Open door Funtion added
	}
	void closeDoor()
	{
	   Close Door funtion Added 
	}
	void lockDoor();
	{
	   Lock Door Funtion Added
	}
	void unlockDoor();
	{
	   UnLock Door Funtion Added
	}
	bool isDoorOpen()const;
	{
	   isOpen=false;
	}
	bool isDoorClose()const
	{
	   isClose=false;
	}
};


int main()
{
	Door myDoor;
	 myDoor.closeDoor();
  	 cout << "Is the door open? " << (myDoor.isDoorOpen() ? "Yes" : "No") << endl;
   	 myDoor.lockDoor();
    	 myDoor.unlockDoor();

	 cout << "Is the door open? " << (myDoor.isDoorOpen() ? "Yes" : "No") << endl;

         myDoor.openDoor();
         cout << "Is the door open? " << (myDoor.isDoorOpen() ? "Yes" : "No") << endl;
	
	return 0;
}