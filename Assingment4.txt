#include<istream>
using namespace std;

class Door{
private:
	bool isOpen;
	bool isClose;
public:
 	Door()
	{
	   constructor funtion
	}

	void openDoor();
	void closeDoor();
	void lockDoor();
	void openDoor();
	void closeDoor();
	void unlockDoor();
	bool isDoorOpen()const;
	bool isDoorClose()const;
};


int main()
{
	Door myDoor;
	 myDoor.closeDoor();
  	 cout << "Is the door open? " << (myDoor.isDoorOpen() ? "Yes" : "No") << endl;
   	 myDoor.lockDoor();
    	 myDoor.unlockDoor();

	 cout << "Is the door open? " << (myDoor.isDoorOpen() ? "Yes" : "No") << endl;

         myDoor.openDoor();
         cout << "Is the door open? " << (myDoor.isDoorOpen() ? "Yes" : "No") << endl;
	
	return 0;
}